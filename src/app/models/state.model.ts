export class State {
  /**
   * Crea una instancia de estado
   * @param {number} id Id del estado
   * @param {string} Nombre Nombre del estado
   * @memberof State
   */
  constructor(
    public id:     number,
    public Nombre: string
  ) {}
}
